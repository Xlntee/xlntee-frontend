import { FC } from "react";

import { Box, Container } from "@mui/material";

const RequestsPage: FC = () => {
  return (
    <Box component="section" py="40px">
      <Container>RequestsPage</Container>
    </Box>
  );
};

export default RequestsPage;
