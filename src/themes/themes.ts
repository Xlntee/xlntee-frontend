import { createTheme } from "@mui/material";

export const defaultTheme = createTheme({
	palette: {
		mode: "light",
		primary: {
			main: "#2D51F7",
		},
		secondary: {
			main: "#35C587",
		},
	},
});
